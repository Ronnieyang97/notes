第二十二章 TCP的坚持定时器
TCP不对ACK报文进行确认，只确认那些包含包含有数据的ACK报文段。如果一个确认丢失了则双方就有可能因为等待对方而使链接终止，接受方等待接收数据，而发送方等待允许他继续发送数据的窗口更新，这种情况下形成死锁。为了防止这种情况，发送方使用一个坚持定时器来周期性的向接收方查询以便发现窗口是否增大，这些从发送方发出的报文段为窗口探查。
基于窗口的流量控制方案，如TCP使用的，会导致一种被称为糊涂窗口综合征的状况，发生时少量的数据将通过连接进行交换，而不是满长度报文段。该现象会发生在任意一端，接收方可以通告一个小窗口，发送方也可以发送少量数据。
